# Dependencies
node_modules/

# Environment / secrets
.env
.env.local

# Firebase service account keys
*-service-account.json
service-account*.json
serviceAccountKey*.json
*.serviceaccount.json

# Figma personal access tokens (if ever written to disk)
.figma-pat

# Generated extraction output (local artifacts, may contain file content)
src/extract/output/

# Built Figma plugin bundle (source of truth is code.ts; run `npm run build:plugin`)
src/plugin/code.js

# SQLite database files — schema.sql is committed, the data is not
*.db
*.sqlite
*.sqlite-shm
*.sqlite-wal
src/store/*.db
src/store/*.sqlite

# OS / editor cruft
.DS_Store
*.log
